.DS_Store
__pycache__/
*.pyc
.pytest_cache/
.env

# Pipeline: local venv, scraped output, downloaded assets.
pipeline/.venv/
pipeline/data/*
!pipeline/data/pushed.json
!pipeline/data/geocache.json
!pipeline/data/excluded.json

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/

# CloudKit server-to-server private key
pipeline/secrets/

# Private keys never belong in the repo; the pipeline keeps its own under pipeline/secrets/.
*.pem
